import { RegistryLabelPipe } from './registry-label.pipe';

describe('RegistryLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new RegistryLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
