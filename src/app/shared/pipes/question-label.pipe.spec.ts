import { QuestionLabelPipe } from './question-label.pipe';

describe('QuestionLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
