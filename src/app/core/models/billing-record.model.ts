export interface BillingRecord {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
}

export interface BillingData {
  url: string;
  orderId: string;
  transactionId: string;
}
